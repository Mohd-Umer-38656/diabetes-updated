import React from "react";
import Register from "@/Components/Register.components"; // Import the Register component

// Define the RegisterPage component
const RegisterPage = () => {
  return (
    <div>
      {/* Render the Register component */}
      <Register />
    </div>
  );
};

// Export the RegisterPage component as default
export default RegisterPage;
